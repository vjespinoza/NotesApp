import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    notesListContainer: {
        columns: "4 225px",
        gap: "1vw",
        width: "80%",
        height: "fit-content",
        position: "relative",
        zIndex: -10,
    },
});

export default useStyles;
