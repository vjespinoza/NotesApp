import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    notesListContainer: {
        columns: "6 15rem",
        columnGap: "0.8rem",
        width: "80%",
        height: "100%",
        position: "relative",
        zIndex: -10,
    },
});

export default useStyles;
